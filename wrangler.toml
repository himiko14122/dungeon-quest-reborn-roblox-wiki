name = "dungeon-quest-reborn-roblox-wiki"
main = "_worker.js"
compatibility_date = "2025-01-01"

[assets]
directory = "./out"
binding = "ASSETS"

# Custom domains (dungeon-quest-reborn-roblox.wiki + www.dungeon-quest-reborn-roblox.wiki) bound via
# Cloudflare Workers Domains API (OAuth). wrangler 4.106 does not
# support a [[domains]] top-level field, so the binding is managed through the API
# (see .codeflicker/state/deployer-return-anime-origins-worker.json).
# CF Worker Builds Git integration configured
